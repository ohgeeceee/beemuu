# BeeEmUu v0.16.1 — Patch Plan

> **Status:** In progress. v0.16.0 shipped 2026-08-31. This patch
> ships the BMW-FAST FMT transport fix (Tier B) + FRM coding dump
> wiring (Tier A).

## Premise

v0.16.0 shipped the feature cycle (i18n, service manual, walk freeze,
vehicle DB, reports, FR i18n, mobile CSS). Two items remain from the
v0.16.0 cycle that didn't land in the main release:

1. **BMW-FAST FMT fix** — a Tier B transport bugfix verified on a
   real 2006 E90 330i. Code is done; needs PR + human merge.
2. **FRM coding dump wiring** — the JS helper (`frm_coding_dump.js`)
   exists but has no tests and is not wired into the Service Functions
   tab UI. Tier A.

## Slices

### Slice 1 — BMW-FAST FMT fix PR (Tier B)

The fix is already implemented and verified. Open a PR with:
- Code changes in `src-tauri/src/transport/kdcan.rs` (BMW-FAST FMT
  byte fix)
- Unit tests pinning the on-wire shape
- Review notes flagging the Tier B protected path

**Acceptance.** `cargo test` passes. Real-car verification documented
(2006 E90 330i, DME answered in ~15 ms, 9 control units found).

### Slice 2 — FRM coding dump tests + wiring (Tier A)

- Add `src/js/test/frm_coding_dump.test.cjs` — test `findFrm`,
  `identLabel`, `sanitizeVin`, `dumpFilename`, `buildDumpText`
- Wire the FRM dump card into `src/index.html` Service Functions tab
  (if not already present)
- Wire the export button in `src/js/main.js` to call `buildDumpText`
  + `invoke('export_text', ...)`

**Acceptance.** `node --test src/js/test/frm_coding_dump.test.cjs`
passes. Service Functions tab shows FRM card with Export button.

## Files to change

```
src-tauri/src/transport/kdcan.rs              # BMW-FAST FMT fix (Tier B)
src/js/test/frm_coding_dump.test.cjs          # new tests
src/index.html                                # FRM card wiring
src/js/main.js                                # export handler
```

## Test plan

- `cd src-tauri && cargo test` — Rust tests including BMW-FAST FMT
- `node --test src/js/test/frm_coding_dump.test.cjs` — JS tests
- `node --test src/js/test/*.test.cjs` — full suite (88+ tests)

*Plan saved 2026-09-01.*
