{
  "name": "beeemuu",
  "version": "0.10.0",
  "description": "Independent OBD2 diagnostic dashboard for BMW vehicles - K+DCAN and ENET",
  "license": "GPL-3.0-or-later",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "test:histogram": "node --test src/js/*.test.js",
    "test:js": "node --test src/js/test/*.test.cjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
