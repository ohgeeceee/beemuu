"use strict";
// Mount the Predictive CBS Timeline UI on the Vehicle Info view. Script runs
// at the end of <body>, so the DOM is ready; the mount is defensive about the
// engine module (window.CbsPredict) being present.
window.mountCbsTimeline();
